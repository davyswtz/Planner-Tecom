# Burrinho Projetos — coloque na raiz do site na HostGator (public_html)
# ou na raiz da pasta do app (ex.: public_html/burrinho/).

DirectoryIndex index.html index.php

AddDefaultCharset utf-8

# ── Segurança básica (HostGator / Apache) ───────────────────────────────────
# (Se algum header não for suportado pelo plano/versão, o Apache pode ignorar.)
<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
  # Força HTTPS por 1 ano (ativar somente após SSL estar ativo no cPanel).
  # Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  # CSP com Leaflet/CARTO + Google Fonts.
  Header always set Content-Security-Policy "default-src 'self'; img-src 'self' data: https:; style-src 'self' 'unsafe-inline' https://unpkg.com https://cdn.jsdelivr.net https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self' 'unsafe-inline' https://unpkg.com https://cdn.jsdelivr.net; connect-src 'self' https:; frame-ancestors 'none'"
</IfModule>

# Não expor arquivos sensíveis por engano
<FilesMatch "(^\.env$|credentials\.php$|credentials\.example\.php$|.*\.sql$|debug_.*\.php$|reset.*\.sql$|DEPLOY_HOSTGATOR\.txt$|NAO_SUBIR_JS_NA_HOSTGATOR\.txt$)">
  Require all denied
</FilesMatch>

# Após SSL ativo no cPanel, descomente para forçar HTTPS:
# RewriteEngine On
# RewriteCond %{HTTPS} !=on
# RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
